{% extends 'base.html.twig' %}

{% block title %}{{'Désintégration' | trans}} - {{'Détails d\'un utilisateur' | trans}}{% endblock %}

{% block body %}
    <div class="row mt-4">
        <div class="col-10 mx-auto">
            <div class="card shadow bg-dark p-4">
                <div class="card-title">
                    <h1 class="text-center">{{'Détails d\'un utilisateur' | trans}}</h1>
                </div>
                <div class="card-body">
                    <table class="table table-dark table-hover">
                        <tbody>
                            <tr class="text-center">
                                <th class="align-middle w-50">{{'Identifiant' | trans}}</th>
                                <td class="align-middle w-50">{{ user.id }}</td>
                            </tr>
                            <tr class="text-center">
                                <th class="align-middle w-50">{{'Nom d\'utilisateur' | trans}}</th>
                                <td class="align-middle w-50">{{ user.username }}</td>
                            </tr>
                            <tr class="text-center">
                                <th class="align-middle w-50">{{'Prénom' | trans}}</th>
                                <td class="align-middle w-50">{{ user.firstName }}</td>
                            </tr>
                            <tr class="text-center">
                                <th class="align-middle w-50">{{'Nom' | trans}}</th>
                                <td class="align-middle w-50">{{ user.lastName }}</td>
                            </tr>
                            <tr class="text-center">
                                <th class="align-middle w-50">{{'Rôles' | trans}}</th>
                                <td class="align-middle w-50">
                                    {% if 'ROLE_SUPER_ADMIN' in user.roles %}
                                        <p class="m-1">{{'Super administrateur' | trans}}</p>
                                    {% endif %}
                                    {% if 'ROLE_ADMIN' in user.roles %}
                                        <p class="m-1">{{'Administrateur' | trans}}</p>
                                    {% endif %}
                                    {% if 'ROLE_USER' in user.roles %}
                                        <p class="m-1">{{'Utilisateur' | trans}}</p>
                                    {% endif %}
                                </td>
                            </tr>
                        </tbody>
                    </table>
                    <div class="d-flex flex-column justify-content-around align-items-center">
                        <div class="card shadow bg-dark p-4">
                            <h3 class='text-center'>{{'Gestion' | trans}}</h3>
                            <div class="d-flex flex-column justify-content-around align-items-center my-2">
                                <a href="{{ path('user_edit', {'id': user.id}) }}"><button class="btn btn-warning">{{'Modifier' | trans}}</button></a>
                                {{ include('user/_delete_form.html.twig') }}
                            </div>
                        </div>
                        <div class="card shadow bg-dark p-4 mt-4">
                            <h3 class='text-center'>{{'Navigation' | trans}}</h3>
                            <div class="d-flex flex-column justify-content-around align-items-center my-2">
                                <a href="{{ path('user_index') }}"><button class="btn btn-primary">{{'Retour à la liste' | trans}}</button></a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
